import { createStore } from 'vuex'
import modules from './module.js'

// export default createStore({
//  modules
// })

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
